/**
 * Sub an integer number value.
 *
 * @param a The integer number value.
 * @param b The integer number value.
 * @returns The integer number value.
 */
export const sub = (a: number, b: number): number => Math.floor(a) - Math.floor(b)
